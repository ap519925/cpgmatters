{#
/**
 * @file
 * Paragraph template: Statistics Bar (cpg_stats_bar)
 * Fields: field_p_title, field_p_items (nested cpg_stat_item)
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    'cpg-paragraph-stats-bar',
  ]
%}
{% block paragraph %}
  <section{{ attributes.addClass(classes) }}>
    {% if content.field_p_title|render|striptags|trim %}
      <h2 class="cpg-paragraph-stats-bar__title">{{ content.field_p_title }}</h2>
    {% endif %}
    <div class="cpg-paragraph-stats-bar__grid">
      {{ content.field_p_items }}
    </div>
  </section>
{% endblock paragraph %}
